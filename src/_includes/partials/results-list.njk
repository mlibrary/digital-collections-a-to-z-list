<h2 class="mb-1">Collection Results</h2>
<p class="results-heading">{{ colllist.items.length }} results</p>

<section class="no-results">
  <div class="callout callout--info">
    <div class="callout__icon">
      <svg xmlns="http://www.w3.org/2000/svg" width="24px" viewBox="0 0 24 24" role="presentation" focusable="false" aria-hidden="true"><path fill="#1D7491" d="M11 17h2v-6h-2zm1-8q.425 0 .713-.288T13 8t-.288-.712T12 7t-.712.288T11 8t.288.713T12 9m0 13q-2.075 0-3.9-.788t-3.175-2.137T2.788 15.9T2 12t.788-3.9t2.137-3.175T8.1 2.788T12 2t3.9.788t3.175 2.137T21.213 8.1T22 12t-.788 3.9t-2.137 3.175t-3.175 2.138T12 22"/></svg>
    </div>
    <div class="callout__message">
    <h3>No results found for your search</h3>
    <p>Try modifying your search.</p>
    <ul>
      <li>Check the spelling of words and names in your search terms.</li>
      <li>Remember, this is not a full-text search, and instead may search 
       descriptions of the materials.</li> 
      <li>Try alternative search terms.</li>
     <li>Consider a different strategy for your search, such as searching a
     broader topic or browsing by subject area.</li>
    </ul>
       <a href="/#find-collection" class="clear-all-btn">Start over to find a collection</a>
    </div>
  </div>
</section>

<ul class="results-list">
  {% for item in colllist.items %}
  <li data-collid="{{ item.collid }}">
    <section class="results-list--grid">
    <img loading="lazy" class="results-list__image" src="https://quod.lib.umich.edu/{{ item.imageURL}}" alt="">
    <div class="results-card">
    <div class="flex flex-flow-column flex-grow-1">
      <h3>
        <a href="{{ item.collhomeURL }}" class="results-link">{{ item.label }}</a>
      </h3>
      <p class="mb-0"> {{ item.description }} </p>
    </div>
  </div>
  <div class="results-dl">
    <dl>
      <div class="results-dl-grid">
        <dt>Format</dt>
        <div>
        {% for format in item.format %}
          <dd>{{ format}}</dd>
        {% endfor %}
        </div>
      </div>
      <div class="results-dl-grid">
        <dt>Access</dt>
        <dd>{{ item.access | capitalize }} </dd>
      </div>
      {% if item.categorylinks != "Collection not in any groups" %}
       <div class="results-dl-grid">
        <dt>Search Within Group</dt>
        <div>
        {% for link in item.categorylinks %}
         <dd> {{ link | safe }} </dd>
        {% endfor %}
        </div>
      </div>
       {% endif %}
    </dl>
    </div>
  </section>
  </li>
  {% endfor %}
</ul>
